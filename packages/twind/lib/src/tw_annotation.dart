/// Annotation for generating Tailwind-style widgets.
class Tw {
  /// Tailwind-style utility classes (space-separated).
  ///
  /// Examples: `bg-red-500 px-4 py-2`.
  final String className;

  const Tw(this.className);
}
